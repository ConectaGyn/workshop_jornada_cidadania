train:
  experiment_name: telco_churn_gb
  model_name: telco_churn_model
  seed: 0
  test_size: 0.2
  n_iter: 30
  n_splits: 5

model:
  learning_rate: 0.5
  n_estimators: 10
  max_depth: 1
  min_samples_split: 50
  min_samples_leaf: 20
